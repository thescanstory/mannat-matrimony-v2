import type { Profile, Scenario } from '../types';

// All mock/demo data removed — admin manages only real user-created profiles.
export const MOCK_SCENARIOS: Scenario[] = [];

export const MOCK_PROFILES: Profile[] = [];
